{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "building an environment",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.mjs",
    "build": "parcel build src/index.html --dist-dir dist --no-source-maps",
    "dev": "live-server src --entry-file=index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "parcel": "^2.12.0"
  }
}
